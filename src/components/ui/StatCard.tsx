export default function StatCard({ title, value, icon, iconBg, subtitle }: { title: string; value: string | number; icon: React.ReactNode; iconBg: string; subtitle?: string }) {
  return (<div className="glass glass-hover" style={{ padding: 18 }}><div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between' }}><div style={{ flex: 1, minWidth: 0 }}><p style={{ fontSize: 10, fontWeight: 700, color: 'var(--text-muted)', textTransform: 'uppercase', letterSpacing: '0.1em', margin: 0 }}>{title}</p><p style={{ fontSize: 24, fontWeight: 800, color: '#fff', margin: '8px 0 0', lineHeight: 1, letterSpacing: '-0.02em' }}>{value}</p>{subtitle && <p style={{ fontSize: 11, color: 'var(--text-muted)', marginTop: 6, lineHeight: 1.5 }}>{subtitle}</p>}</div><div style={{ width: 40, height: 40, borderRadius: 10, display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0, marginLeft: 10, background: iconBg }}>{icon}</div></div></div>)
}